# 2026-06-11 开发日志

## 今日完成

### 前端页面全面美化（类 Gemini/Modern Design 风格）

- **全局主题重构**：引入 Inter 字体 + CSS 变量体系，统一了色板、阴影、圆角，覆盖 Element Plus 默认样式（表格、卡片、对话框、按钮、输入框、消息提示）。

- **登录页重设计**：深色渐变背景 + 动态光晕动画，磨砂玻璃质感卡片，品牌图标区，圆角输入框带聚焦紫色边框，渐变登录按钮带发光阴影。

- **侧边栏与布局优化**：深色侧边栏 `#1e293b`，带渐变紫色方形 Logo，菜单项圆角悬停高亮，毛玻璃头部导航栏（`backdrop-filter: blur(12px)`），用户头像首字母 + 下拉退出。

- **仪表盘高光打造**：
  - 动态欢迎语（按时段问候）+ 当前日期
  - 4 张渐变色功能卡片（员工=紫粉、供应商=橙黄、商品=蓝紫、采购=绿青），hover 上浮效果
  - 库存表格增加商品图标列 + 库存进度条（绿=充足、蓝=正常、黄=较少、红=紧缺）+ 统计面板

- **列表页统一美化**：每个页面对应彩色图标方块标题区，表格单元格间距优化。"操作"列编辑/删除按钮改为纯色文字+hover 背景、flex 容器同行排列，解决上下错位问题。

- **采购模块完善**：经办人列改为显示员工姓名、新增员工编号列；采购明细表新增商品名称列；编辑明细后自动重算采购主表总数量和总金额（后端 SQL 自动汇总）。

- **弹窗表格布局修正**：逐步调大新增/编辑采购单弹窗宽度（840→1100px），各列宽度重新分配，`el-input-number` 使用 `controls-position="right"` 修复 +/- 按钮被裁切问题，覆盖 Element Plus 表格默认 `text-overflow: ellipsis` 防止文字截断。

> 已产出：全局样式文件 (`style.css`)、布局组件 (`MainLayout.vue`)、登录页 (`LoginView.vue`)、仪表盘 (`DashboardView.vue`)、员工/供应商/商品/采购列表页及明细页的全面翻新。
