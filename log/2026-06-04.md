# 2026-06-04 开发日志

## 今日完成
- **持久层（Mapper）全线完工**：
  - 严格根据《数据库课设题目》的原生需求，补齐并修正了全部 5 个核心模块（员工、供应商、商品、采购单、采购明细）的 Mapper 接口。
  - 在 `resources/mapper` 下完整编写了所有对应的 XML 映射文件。
- **商品模块结构精准对齐**：
  - 根据本地物理数据库 `product` 表的真实结构，在 `ProductMapper.xml` 中精准对齐了 `p_id`、`p_name`、`p_price`、`s_id`、`p_intro`、`p_remark` 这 6 个真实字段，删除了冗余设计，确保底层 SQL 100% 运行合规。
- **业务层（Service）进度过半**：
  - 成功搭建了 `service` 核心业务层架构。
  - 已经完成了员工模块（`EmployeeService` 及 `EmployeeServiceImpl`）的业务逻辑编写，并额外扩展了“管理员/普通用户登录验证”的核心功能。
  - 顺利展开了供应商、商品、采购等后续模块的 Service 接口与实现类编写。

## 遇到的问题与解决方案
- **问题：数据库字段与后端 SQL 预期不一致**
  - **描述**：在编写商品（Product）增删改查 SQL 时，起初使用了通用的条形码和单位字段，但对比本地 Navicat 数据库后，发现本地实际设计的是 `p_intro`（商品简介）。如果直接运行会导致数据库抛出“未知列（Unknown column）”异常。
  - **解决**：立即根据本地物理表结构，将 `ProductMapper.xml` 里的 `INSERT`、`UPDATE` 和 `SELECT` 语句全部进行了重写，使其与数据库字段、Java 实体类属性完美闭环，排除了运行时隐患。

## 明日计划
- **收尾 Service 业务层**：将剩余模块（供应商、商品、采购单及明细）的 Service 实现类全部编写完毕，完成超市系统核心业务逻辑的闭环。
- **平推 Controller 控制层**：创建 `controller` 包，将 5 个模块的业务全部包装成标准的 HTTP 接口，允许前端进行跨域（@CrossOrigin）调用。
- **后端终极总测**：启动 Spring Boot 主程序，进行全系统接口连通性测试，宣告后端开发全线大结局。