# 数据库实验报告

> **课程名称**：数据库系统概论课程设计  
> **项目名称**：超市进销存管理系统  
> **姓 名**：文泽仁  
> **班 级**：软件工程241  
> **学 号**：2024082123  
> **实验日期**：2026年6月  

---

## 一、实验目的

1. 掌握关系型数据库的设计方法，能根据实际业务需求设计合理的 E-R 模型。
2. 熟练使用 MySQL 进行数据库的创建、表结构定义及约束设置。
3. 掌握 SQL 语言的核心操作：数据定义（DDL）、数据操纵（DML）和数据查询（DQL）。
4. 理解外键约束与表间关联关系，能实现多表级联操作。
5. 将数据库理论与实际项目（超市进销存管理系统）相结合，完成从前端到后端的完整数据链路。

---

## 二、需求分析

### 2.1 业务背景

某中型连锁超市需要对日常的**供应商管理、商品管理、员工管理和采购管理**进行信息化改造。当前存在以下问题：

- 供应商信息分散记录在纸质档案或 Excel 中，查找不便；
- 商品信息与供应商脱节，难以追溯货源；
- 采购订单缺乏系统性记录，历史数据无法查询；
- 员工信息管理混乱，权限不分明。

### 2.2 功能需求

| 模块 | 功能描述 |
|---|---|
| **供应商管理** | 录入、查询、修改、删除供应商信息 |
| **商品管理** | 录入、查询、修改、删除商品信息，关联供应商 |
| **员工管理** | 录入、查询、修改、删除员工信息，区分管理员与普通员工 |
| **采购管理** | 录入采购订单（主表+明细），查询、修改、级联删除 |

### 2.3 数据需求

- 系统需存储 **5 类核心数据**：供应商、商品、员工、采购订单、采购明细
- 供应商与商品之间为 **一对多** 关系（一个供应商可供应多种商品）
- 采购订单与采购明细之间为 **一对多** 关系（一个订单包含多个商品明细）
- 员工与采购订单之间为 **一对多** 关系（一个员工可创建多个采购订单）
- 商品与采购明细之间为 **一对多** 关系（一种商品可出现在多条采购明细中）

---

## 三、概念结构设计（E-R 模型）

### 3.1 实体定义

系统包含 5 个实体：

| 实体 | 主键 | 主要属性 |
|---|---|---|
| **供应商（supplier）** | s_id | 名称、简称、地址、电话、邮箱、联系人、联系人电话、备注 |
| **商品（product）** | p_id | 名称、单价、供应商编号(外键)、简介、备注 |
| **员工（employee）** | e_id | 姓名、密码、级别、电话、工资、备注 |
| **采购订单（purchase_order）** | o_id | 员工编号(外键)、采购总量、采购总价、采购时间、备注 |
| **采购明细（purchase_detail）** | d_id | 订单编号(外键)、商品编号(外键)、数量、单价、总价、备注 |

### 3.2 实体间关系

```
supplier ────1:N──── product ────1:N──── purchase_detail ────N:1──── purchase_order ────N:1──── employee
```

- **供应商 → 商品**：一个供应商供应多种商品（1 : N）
- **商品 → 采购明细**：一种商品可出现在多条采购明细中（1 : N）
- **采购订单 → 采购明细**：一个订单包含多条采购明细（1 : N）
- **员工 → 采购订单**：一个员工可创建多个采购订单（1 : N）

---

## 四、逻辑结构设计

### 4.1 数据库信息

- **数据库名称**：`supermarket_db`
- **数据库引擎**：MySQL 8.0
- **字符集**：utf8mb4（兼容 emoji 及特殊中文）

### 4.2 表结构设计

#### 4.2.1 供应商表（supplier）

| 字段名 | 数据类型 | 主键 | 非空 | 说明 |
|---|---|---|---|---|
| s_id | VARCHAR(20) | ✅ | ✅ | 供应商编号 |
| s_name | VARCHAR(100) | | ✅ | 供应商名称 |
| s_short_name | VARCHAR(50) | | | 供应商简称 |
| s_address | VARCHAR(200) | | | 公司地址 |
| s_phone | VARCHAR(20) | | | 公司电话 |
| s_email | VARCHAR(50) | | | 电子邮箱 |
| s_contact_person | VARCHAR(50) | | | 联系人 |
| s_contact_phone | VARCHAR(20) | | | 联系人电话 |
| s_remark | VARCHAR(500) | | | 备注 |

#### 4.2.2 商品表（product）

| 字段名 | 数据类型 | 主键 | 非空 | 说明 |
|---|---|---|---|---|
| p_id | VARCHAR(20) | ✅ | ✅ | 商品编号 |
| p_name | VARCHAR(100) | | ✅ | 商品名称 |
| p_price | DECIMAL(10,2) | | ✅ | 商品单价 |
| s_id | VARCHAR(20) | | ✅ | 供应商编号（外键） |
| p_intro | VARCHAR(500) | | | 商品简介 |
| p_remark | VARCHAR(500) | | | 备注 |

> **外键约束**：`FOREIGN KEY (s_id) REFERENCES supplier(s_id)`

#### 4.2.3 员工表（employee）

| 字段名 | 数据类型 | 主键 | 非空 | 说明 |
|---|---|---|---|---|
| e_id | VARCHAR(20) | ✅ | ✅ | 员工编号 |
| e_name | VARCHAR(50) | | ✅ | 员工姓名 |
| e_password | VARCHAR(50) | | ✅ | 登录密码 |
| e_level | VARCHAR(20) | | ✅ | 级别（管理员/普通员工） |
| e_phone | VARCHAR(20) | | | 联系电话 |
| e_salary | DECIMAL(10,2) | | | 工资 |
| e_remark | VARCHAR(500) | | | 备注 |

#### 4.2.4 采购订单表（purchase_order）

| 字段名 | 数据类型 | 主键 | 非空 | 说明 |
|---|---|---|---|---|
| o_id | VARCHAR(20) | ✅ | ✅ | 采购清单号 |
| e_id | VARCHAR(20) | | ✅ | 员工编号（外键） |
| o_total_quantity | INT | | | 采购总数量 |
| o_total_price | DECIMAL(12,2) | | | 采购总价 |
| o_time | DATETIME | | | 采购时间 |
| o_remark | VARCHAR(500) | | | 备注 |

> **外键约束**：`FOREIGN KEY (e_id) REFERENCES employee(e_id)`

#### 4.2.5 采购明细表（purchase_detail）

| 字段名 | 数据类型 | 主键 | 非空 | 说明 |
|---|---|---|---|---|
| d_id | VARCHAR(20) | ✅ | ✅ | 明细编号 |
| o_id | VARCHAR(20) | | ✅ | 订单编号（外键） |
| p_id | VARCHAR(20) | | ✅ | 商品编号（外键） |
| d_quantity | INT | | | 采购数量 |
| d_price | DECIMAL(10,2) | | | 采购单价 |
| d_total_price | DECIMAL(12,2) | | | 明细总价 |
| d_remark | VARCHAR(500) | | | 备注 |

> **外键约束**：`FOREIGN KEY (o_id) REFERENCES purchase_order(o_id)`，`FOREIGN KEY (p_id) REFERENCES product(p_id)`

---

## 五、物理结构设计（SQL 建表脚本）

```sql
-- ============================================
-- 超市进销存管理系统 — 数据库建表脚本
-- 数据库名称：supermarket_db
-- ============================================

CREATE DATABASE IF NOT EXISTS supermarket_db
DEFAULT CHARACTER SET utf8mb4
DEFAULT COLLATE utf8mb4_unicode_ci;

USE supermarket_db;

-- 1. 供应商表
CREATE TABLE IF NOT EXISTS supplier (
    s_id            VARCHAR(20)   PRIMARY KEY,
    s_name          VARCHAR(100)  NOT NULL,
    s_short_name    VARCHAR(50),
    s_address       VARCHAR(200),
    s_phone         VARCHAR(20),
    s_email         VARCHAR(50),
    s_contact_person VARCHAR(50),
    s_contact_phone VARCHAR(20),
    s_remark        VARCHAR(500)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 2. 商品表
CREATE TABLE IF NOT EXISTS product (
    p_id    VARCHAR(20)    PRIMARY KEY,
    p_name  VARCHAR(100)   NOT NULL,
    p_price DECIMAL(10,2)  NOT NULL,
    s_id    VARCHAR(20)    NOT NULL,
    p_intro VARCHAR(500),
    p_remark VARCHAR(500),
    FOREIGN KEY (s_id) REFERENCES supplier(s_id)
        ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 3. 员工表
CREATE TABLE IF NOT EXISTS employee (
    e_id       VARCHAR(20)    PRIMARY KEY,
    e_name     VARCHAR(50)    NOT NULL,
    e_password VARCHAR(50)    NOT NULL,
    e_level    VARCHAR(20)    NOT NULL DEFAULT '普通员工',
    e_phone    VARCHAR(20),
    e_salary   DECIMAL(10,2),
    e_remark   VARCHAR(500)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 4. 采购订单表（主表）
CREATE TABLE IF NOT EXISTS purchase_order (
    o_id             VARCHAR(20)    PRIMARY KEY,
    e_id             VARCHAR(20)    NOT NULL,
    o_total_quantity INT,
    o_total_price    DECIMAL(12,2),
    o_time           DATETIME,
    o_remark         VARCHAR(500),
    FOREIGN KEY (e_id) REFERENCES employee(e_id)
        ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 5. 采购明细表（子表）
CREATE TABLE IF NOT EXISTS purchase_detail (
    d_id          VARCHAR(20)    PRIMARY KEY,
    o_id          VARCHAR(20)    NOT NULL,
    p_id          VARCHAR(20)    NOT NULL,
    d_quantity    INT,
    d_price       DECIMAL(10,2),
    d_total_price DECIMAL(12,2),
    d_remark      VARCHAR(500),
    FOREIGN KEY (o_id) REFERENCES purchase_order(o_id)
        ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (p_id) REFERENCES product(p_id)
        ON DELETE RESTRICT ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 关键约束说明

| 约束类型 | 表 | 关联 | 策略 |
|---|---|---|---|
| 外键 | product.s_id → supplier.s_id | RESTRICT | 有商品的供应商不可直接删除 |
| 外键 | purchase_order.e_id → employee.e_id | RESTRICT | 有订单记录的员工不可直接删除 |
| 外键 | purchase_detail.o_id → purchase_order.o_id | CASCADE | 删订单时自动级联删除明细 |
| 外键 | purchase_detail.p_id → product.p_id | RESTRICT | 已被采购的商品不可直接删除 |

---

## 六、数据操纵测试

### 6.1 初始数据插入

```sql
-- 插入管理员
INSERT INTO employee VALUES
('E001', '麦克斯', '123456', '管理员', '18888889999', 8000.00, '采购经理');

-- 插入供应商
INSERT INTO supplier VALUES
('S001', '可口可乐供应商有限公司', '可乐供销', '北京市海淀区',
 '010-12345678', 'cola@test.com', '张经理', '13811112222', '碳酸饮料供应商');

-- 插入商品
INSERT INTO product VALUES
('P001', '可口可乐500ml', 3.00, 'S001', '经典碳酸饮料', '畅销品');
```

### 6.2 核心查询

```sql
-- 查询所有商品及其供应商名称（多表连接）
SELECT p.p_id, p.p_name, p.p_price, s.s_name AS supplier_name
FROM product p
LEFT JOIN supplier s ON p.s_id = s.s_id;

-- 查询某订单的完整信息（三表连接）
SELECT o.o_id, o.o_time, e.e_name,
       d.p_id, d.d_quantity, d.d_price, d.d_total_price
FROM purchase_order o
JOIN employee e ON o.e_id = e.e_id
JOIN purchase_detail d ON o.o_id = d.o_id
WHERE o.o_id = 'PO20260605001';

-- 统计各供应商的商品数量
SELECT s.s_name, COUNT(p.p_id) AS product_count
FROM supplier s
LEFT JOIN product p ON s.s_id = p.s_id
GROUP BY s.s_id, s.s_name;
```

---

## 七、实验小结

### 7.1 收获

1. **需求分析能力**：学会了从业务描述中提取实体、属性和关系的方法。
2. **E-R 建模能力**：掌握了概念模型到逻辑模型的转换过程，以及外键策略的选择（RESTRICT vs CASCADE）。
3. **SQL 实践能力**：熟练掌握了 DDL、DML、DQL 的编写，尤其对多表连接查询和聚合函数有了更深理解。
4. **工程化意识**：经历了"需求分析 → E-R 设计 → 建表 → 后端实现 → 接口测试"的完整流程，对软件开发全生命周期有了直观认识。

### 7.2 存在的问题与改进方向

1. **缺少索引优化**：对于高频查询字段（如 `p_name`、`o_time`），后续可增加索引提升查询性能。
2. **密码明文存储**：出于课设简化的考虑，员工密码目前以明文存储，实际生产环境应使用 MD5/SHA256 等哈希加密。
3. **分页查询**：当前查询接口未做分页处理，数据量增大后需引入 `LIMIT/OFFSET` 分页机制。
