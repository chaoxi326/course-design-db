# 超市进销存管理系统 — 项目设计说明书

> **课程名称**：数据库原理与应用课程设计
> **项目名称**：基于 Spring Boot 的超市进销存管理系统的设计与实现
> **姓    名**：陈卓熙
> **学    院**：信息与通信工程学院
> **专    业**：通信工程

---

## 一、项目概述

### 1.1 项目背景

随着零售行业的快速发展，中小型超市对信息化管理的需求日益增长。传统的手工记账或 Excel 管理方式存在效率低、易出错、数据难以追溯、统计困难等问题。本项目旨在开发一套基于 **B/S 架构**的超市进销存管理系统，采用前后端分离架构，实现供应商、商品、员工、采购订单及明细的数字化管理，并创新性地集成大语言模型 API 提供 AI 智能经营分析功能。

### 1.2 项目目标

- 建立规范的关系型数据库（5 张核心表），完整定义主键、外键、级联约束
- 基于 Spring Boot 实现 RESTful API，覆盖 4 大业务模块的增删改查
- 基于 Vue 3 + Element Plus 实现美观、响应式的前端管理界面
- 实现基于 BCrypt 的密码加密存储与登录认证、基于角色的权限控制
- 实现采购订单与明细的级联操作（级联新增、级联删除、汇总自动刷新）
- 集成 DeepSeek 大语言模型 API，基于真实经营数据生成 AI 分析报告

### 1.3 运行环境

| 类别 | 技术 | 版本 |
|------|------|------|
| 操作系统 | Windows 11 | — |
| 数据库 | MySQL (InnoDB) | 8.0 |
| JDK | OpenJDK | 21 |
| 构建工具 | Maven | 3.9+ |
| 后端框架 | Spring Boot | 3.4.1 |
| 持久层框架 | MyBatis | 3.0.4 |
| 安全 | spring-security-crypto (BCrypt) | — |
| 前端框架 | Vue 3 (Composition API) | 3.5 |
| UI 组件库 | Element Plus | 2.14 |
| 构建工具 | Vite | 8 |
| 状态管理 | Pinia | 3 |
| AI 集成 | DeepSeek API (Chat Completions) | — |
| 开发工具 | IntelliJ IDEA / VS Code | — |

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端 (Vue 3 + Element Plus)            │
│                   Vite 开发服务器 :5173                    │
│  ┌─────────┐ ┌──────────┐ ┌─────────┐ ┌──────────┐     │
│  │ 登录页  │ │  仪表盘   │ │ 列表页  │ │  详情页   │     │
│  │ Login   │ │Dashboard │ │ List×4  │ │ Detail   │     │
│  └─────────┘ └──────────┘ └─────────┘ └──────────┘     │
│          Pinia (user store) + Vue Router (auth guard)    │
└──────────────────────┬──────────────────────────────────┘
                       │ HTTP / JSON (Axios) + Vite Proxy
┌──────────────────────▼──────────────────────────────────┐
│                Controller 层 (REST API)                   │
│  EmployeeController  SupplierController                  │
│  ProductController  PurchaseController                   │
│  AnalysisController                                      │
│            端口: 8080  /api/*  (17 个接口)                 │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│                Service 层 (业务逻辑)                       │
│  EmployeeService  SupplierService  ProductService         │
│  PurchaseService  AnalysisService                        │
│  BCrypt 密码加密 | @Transactional 事务                    │
│  预计算汇总 | Prompt 构建 | DeepSeek API 调用              │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│                Mapper 层 (数据访问)                        │
│  EmployeeMapper  SupplierMapper  ProductMapper            │
│  PurchaseOrderMapper  PurchaseDetailMapper                │
│         MyBatis XML 映射 (5 个文件, 17 条 SQL)            │
└──────────────────────┬──────────────────────────────────┘
                       │ JDBC
┌──────────────────────▼──────────────────────────────────┐
│              MySQL 数据库 (supermarket_db)                 │
│  InnoDB 引擎 | utf8mb4 字符集 | 5 张核心表 + 4 条外键      │
└─────────────────────────────────────────────────────────┘
```

### 2.2 项目目录结构

```
course-design-db/
├── 数据库/
│   ├── init.sql                  # 建库 + 建表 + 测试数据
│   └── images/er.jpg             # E-R 图
├── 后端服务代码/supermarket/
│   ├── pom.xml
│   └── src/main/
│       ├── java/com/example/supermarket/
│       │   ├── SupermarketApplication.java
│       │   ├── config/
│       │   │   ├── CorsConfig.java
│       │   │   └── SecurityConfig.java    # BCrypt Bean
│       │   ├── common/
│       │   │   ├── Result.java            # 统一响应体
│       │   │   ├── ResultCode.java        # 状态码枚举
│       │   │   └── GlobalExceptionHandler.java
│       │   ├── entity/
│       │   │   ├── Employee.java
│       │   │   ├── Product.java
│       │   │   ├── Supplier.java
│       │   │   ├── PurchaseOrder.java
│       │   │   ├── PurchaseDetail.java
│       │   │   ├── AnalysisRequest.java   # AI 分析请求 DTO
│       │   │   └── AnalysisResponse.java  # AI 分析响应 DTO
│       │   ├── mapper/                    # 5 个 Mapper 接口
│       │   ├── service/                   # 6 个接口
│       │   ├── service/impl/              # 6 个实现
│       │   └── controller/                # 5 个控制器
│       └── resources/
│           ├── application.yaml
│           ├── mapper/                    # 5 个 XML 映射
│           └── db/
│               ├── schema.sql
│               └── data.sql
├── 前端代码/
│   ├── package.json
│   ├── vite.config.js
│   └── src/
│       ├── App.vue
│       ├── main.js
│       ├── style.css                     # 全局主题
│       ├── api/                          # 5 个 API 模块
│       ├── router/index.js               # 路由 + 权限守卫
│       ├── stores/user.js                # Pinia 状态
│       ├── layout/MainLayout.vue         # 布局组件
│       └── views/
│           ├── login/LoginView.vue
│           ├── dashboard/DashboardView.vue  # 含 AI 分析
│           ├── employee/EmployeeList.vue
│           ├── employee/EmployeeInfo.vue
│           ├── supplier/SupplierList.vue
│           ├── product/ProductList.vue
│           ├── purchase/PurchaseList.vue
│           └── purchase/PurchaseDetail.vue
├── log/                                # 6 篇开发日志
├── doc/
│   ├── 超市进销存管理系统_课程设计报告.docx
│   ├── 数据库ER图.docx
│   └── AI分析功能技术文档.md
├── generate_er.py                      # ER 图生成脚本
├── generate_report.py                  # 报告生成脚本
└── README.md
```

---

## 三、数据库设计

### 3.1 数据库基本信息

| 项目 | 内容 |
|------|------|
| 数据库名称 | `supermarket_db` |
| 引擎 | InnoDB |
| 字符集 | utf8mb4 |
| 表数量 | 5 张 |
| 外键约束 | 4 条 |
| 级联删除 | 1 条 (purchase_detail.o_id) |

### 3.2 E-R 关系图

```
 ┌─────────────────┐          ┌─────────────────┐
 │    supplier      │          │    employee      │
 │─────────────────│          │─────────────────│
 │ 🔑 s_id  (PK)   │          │ 🔑 e_id  (PK)   │
 │    s_name       │          │    e_name       │
 │    s_phone      │          │    e_password   │ (BCrypt)
 │    s_email      │          │    e_level      │
 │    ...          │          │    e_phone      │
 └────────┬────────┘          │    e_salary     │
          │                   └────────┬────────┘
          │ 1:N                        │ 1:N
          │                            │
          ▼                            ▼
 ┌─────────────────┐          ┌─────────────────┐
 │    product       │          │ purchase_order  │
 │─────────────────│          │─────────────────│
 │ 🔑 p_id  (PK)   │          │ 🔑 o_id  (PK)   │
 │    p_name       │          │ 🔗 e_id  (FK)   │
 │    p_price      │          │    o_total_qty  │
 │ 🔗 s_id  (FK)   │          │    o_total_price│
 │    p_intro      │          │    o_time       │
 │    p_remark     │          │    o_remark     │
 └────────┬────────┘          └────────┬────────┘
          │                            │
          │ 1:N                        │ 1:N (CASCADE)
          │                            │
          └──────────┬─────────────────┘
                     │
                     ▼
          ┌─────────────────────┐
          │   purchase_detail   │
          │─────────────────────│
          │ 🔑 d_id     (PK)    │
          │ 🔗 o_id     (FK)    │
          │ 🔗 p_id     (FK)    │
          │    d_quantity       │
          │    d_price          │
          │    d_total_price    │
          │    d_remark         │
          └─────────────────────┘
```

### 3.3 数据库表结构

#### supplier（供应商表）

| 字段名 | 数据类型 | 是否为空 | 约束 | 说明 |
|--------|----------|----------|------|------|
| s_id | VARCHAR(20) | NOT NULL | PRIMARY KEY | 供应商编号 |
| s_name | VARCHAR(100) | NOT NULL | — | 供应商名称 |
| s_short_name | VARCHAR(50) | DEFAULT NULL | — | 供应商简称 |
| s_address | VARCHAR(200) | DEFAULT NULL | — | 地址 |
| s_phone | VARCHAR(20) | DEFAULT NULL | — | 公司电话 |
| s_email | VARCHAR(100) | DEFAULT NULL | — | 邮件 |
| s_contact_person | VARCHAR(50) | DEFAULT NULL | — | 联系人 |
| s_contact_phone | VARCHAR(20) | DEFAULT NULL | — | 联系人电话 |
| s_remark | VARCHAR(500) | DEFAULT NULL | — | 备注 |

#### product（商品表）

| 字段名 | 数据类型 | 是否为空 | 约束 | 说明 |
|--------|----------|----------|------|------|
| p_id | VARCHAR(20) | NOT NULL | PRIMARY KEY | 商品编号 |
| p_name | VARCHAR(100) | NOT NULL | — | 商品名称 |
| p_price | DECIMAL(10,2) | NOT NULL | — | 商品单价 |
| s_id | VARCHAR(20) | NOT NULL | FK→supplier(s_id) | 供应商编号 |
| p_intro | VARCHAR(500) | DEFAULT NULL | — | 商品简介 |
| p_remark | VARCHAR(500) | DEFAULT NULL | — | 备注 |

#### employee（员工表）

| 字段名 | 数据类型 | 是否为空 | 约束 | 说明 |
|--------|----------|----------|------|------|
| e_id | VARCHAR(20) | NOT NULL | PRIMARY KEY | 员工编号（工号） |
| e_name | VARCHAR(50) | NOT NULL | — | 员工姓名 |
| e_password | VARCHAR(255) | NOT NULL | — | 密码（BCrypt 加密） |
| e_level | VARCHAR(20) | DEFAULT NULL | — | 级别（管理员/普通员工） |
| e_phone | VARCHAR(20) | DEFAULT NULL | — | 电话 |
| e_salary | DECIMAL(10,2) | DEFAULT NULL | — | 工资 |
| e_remark | VARCHAR(500) | DEFAULT NULL | — | 备注 |

#### purchase_order（采购订单主表）

| 字段名 | 数据类型 | 是否为空 | 约束 | 说明 |
|--------|----------|----------|------|------|
| o_id | VARCHAR(20) | NOT NULL | PRIMARY KEY | 采购清单号 |
| e_id | VARCHAR(20) | NOT NULL | FK→employee(e_id) | 经办员工编号 |
| o_total_quantity | INT | NOT NULL | — | 采购总数量（冗余汇总） |
| o_total_price | DECIMAL(12,2) | NOT NULL | — | 采购总价（冗余汇总） |
| o_time | DATETIME | DEFAULT NULL | — | 采购时间 |
| o_remark | VARCHAR(500) | DEFAULT NULL | — | 备注 |

#### purchase_detail（采购明细表）

| 字段名 | 数据类型 | 是否为空 | 约束 | 说明 |
|--------|----------|----------|------|------|
| d_id | VARCHAR(20) | NOT NULL | PRIMARY KEY | 明细编号 |
| o_id | VARCHAR(20) | NOT NULL | FK→purchase_order(o_id) ON DELETE CASCADE | 订单编号 |
| p_id | VARCHAR(20) | NOT NULL | FK→product(p_id) | 商品编号 |
| d_quantity | INT | NOT NULL | — | 采购数量 |
| d_price | DECIMAL(10,2) | NOT NULL | — | 采购单价 |
| d_total_price | DECIMAL(12,2) | NOT NULL | — | 采购小计 |
| d_remark | VARCHAR(500) | DEFAULT NULL | — | 备注 |

### 3.4 关键设计点

1. **purchase_detail 是核心关联表**，通过 `o_id` 和 `p_id` 双向桥接采购订单与商品
2. **purchase_detail.o_id 设置 ON DELETE CASCADE**：删除采购单时自动级联删除所有明细行
3. **purchase_order 的 o_total_quantity 和 o_total_price 为冗余汇总字段**，由采购明细表的 SUM 子查询实时自动计算刷新
4. **employee.e_password 采用 BCrypt 单向加密**，数据库不存储明文
5. **所有主键和外键由 InnoDB 自动建立索引**

---

## 四、功能模块设计

### 4.1 登录与权限控制

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 登录 | POST | `/api/employees/login` | 工号+密码，BCrypt 验证 |

- 前端路由守卫 `router.beforeEach` 检查 `isLoggedIn` 和角色匹配
- 管理员可访问全部功能，普通员工只能查看仪表盘和个人信息
- 用户信息存储在 Pinia store + localStorage

### 4.2 员工管理模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 查询全部 | GET | `/api/employees` | 返回所有员工（管理员） |
| 批量录入 | POST | `/api/employees/batch` | JSON 数组，密码自动 BCrypt 加密 |
| 修改信息 | PUT | `/api/employees` | 密码留空则保留原密码 |
| 删除 | DELETE | `/api/employees/{eId}` | 有采购记录的员工受外键约束 |

### 4.3 供应商管理模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 查询全部 | GET | `/api/suppliers` | 返回所有供应商 |
| 批量录入 | POST | `/api/suppliers/batch` | JSON 数组 |
| 修改信息 | PUT | `/api/suppliers` | 根据 sId 更新 |
| 删除 | DELETE | `/api/suppliers/{sId}` | 有商品关联时受外键约束 |

### 4.4 商品管理模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 查询全部 | GET | `/api/products` | 返回所有商品 |
| 批量录入 | POST | `/api/products/batch` | JSON 数组，校验单价 > 0 |
| 修改信息 | PUT | `/api/products` | 根据 pId 更新 |
| 删除 | DELETE | `/api/products/{pId}` | 已被采购的商品受外键约束 |

### 4.5 采购管理模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 查询主表 | GET | `/api/purchase/orders` | 返回所有采购单 |
| 查询明细 | GET | `/api/purchase/details` | 返回所有明细 |
| 录入采购单 | POST | `/api/purchase/batch` | 同时提交 orders[] 和 details[]，事务保证 |
| 修改主表 | PUT | `/api/purchase/order` | 编辑经办员工、时间、备注 |
| 修改明细 | PUT | `/api/purchase/detail` | 修改数量/商品，自动刷新主表汇总 |
| 刷新汇总 | PUT | `/api/purchase/refresh/{oId}` | SQL 子查询 SUM 重算 |
| 级联删除 | DELETE | `/api/purchase/{oId}` | 先删明细，再删主表（事务 + CASCADE） |

### 4.6 仪表盘

| 功能 | 说明 |
|------|------|
| 欢迎语 | 按时段动态问候（早上/上午/中午/下午/晚上） |
| 功能导航 | 渐变色卡片，按角色动态展示 |
| 库存概览 | 采购量汇总表格 + 进度条可视化 + 库存状态标签 |
| **AI 智能分析** | 点击按钮 → DeepSeek API 生成中文 Markdown 经营分析报告 |

### 4.7 AI 分析模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| AI 分析 | POST | `/api/analysis` | 收集全量数据 + 预计算 + 调 DeepSeek |

**分析维度**：
1. 📦 商品采购分析 — 采购量排名、热门商品、趋势
2. 🏭 供应商分析 — 供货量排名、占比分析
3. ⚠️ 库存预警与补货建议 — 低于中位数标记、补货优先级
4. 👤 员工绩效分析 — 采购贡献排名、活跃度

**实现流程**：全量数据收集 → Java 端预计算（Top 15、中位数预警、排名汇总）→ 构建结构化 Prompt → DeepSeek Chat Completions API → 解析 Markdown 报告 → 前端正则渲染

---

## 五、技术要点

### 5.1 统一响应体

所有 API 返回统一 JSON 格式：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": { ... }
}
```

`Result<T>` 泛型类，支持 `success()`、`fail()`、`error()`、`unauthorized()` 等工厂方法。

### 5.2 全局异常处理

`@RestControllerAdvice` 统一拦截异常：
- `DataAccessException` → 500 + 通用提示（不泄露 SQL 细节）
- `IllegalArgumentException` → 400 + 具体错误信息（用于业务校验）
- `IllegalStateException` → 500 + 友好提示（用于 AI 服务未配置等场景）
- `Exception` → 500 + 记录日志（不泄露内部细节）

### 5.3 密码安全

- 使用 `BCryptPasswordEncoder` 加密存储
- 录入时自动加密，登录时 `matches()` 验证
- 修改员工时密码为空则跳过加密（可改其他信息但保留密码）
- 查询员工列表时排除密码字段传给前端（`setEPassword(null)`）

### 5.4 事务管理

采购模块核心操作使用 `@Transactional`：

```java
// 采购明细修改 + 汇总刷新（要么全成功，要么全回滚）
@Transactional
public boolean modifyDetail(PurchaseDetail detail) {
    boolean ok = detailMapper.updateDetail(detail) > 0;
    if (ok) {
        int updated = orderMapper.updateOrderTotals(detail.getOId());
        if (updated == 0) throw new IllegalStateException("采购单不存在");
    }
    return ok;
}
```

### 5.5 MyBatis 批量操作与条件更新

- **批量 INSERT**：`<foreach>` 动态生成多行 VALUES
- **条件 UPDATE**：`<set>` + `<if test="...">` 防止空值覆盖已有数据（如修改员工时不改密码则密码保持原值）

### 5.6 CORS 跨域配置

```java
registry.addMapping("/**")
    .allowedOriginPatterns("*")
    .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
    .allowedHeaders("*")
    .maxAge(3600);
```

### 5.7 AI API 集成

```java
// DeepSeek API (OpenAI 兼容 Chat Completions 格式)
RestClient client = RestClient.builder()
    .requestFactory(factory)  // connectTimeout=30s, readTimeout=120s
    .build();

Map<String, Object> body = Map.of(
    "model", "deepseek-chat",
    "max_tokens", 8192,
    "messages", List.of(
        Map.of("role", "system", "content", systemPrompt),
        Map.of("role", "user", "content", userMessage)
    )
);

Map<String, Object> response = client.post()
    .uri("https://api.deepseek.com/chat/completions")
    .header("Authorization", "Bearer " + apiKey)
    .body(body)
    .retrieve()
    .body(Map.class);
```

### 5.8 前端 Markdown 渲染

零第三方依赖，使用正则表达式将 Markdown 实时转换为 HTML：支持 h1-h4 标题、粗体、无序列表、表格（含斑马纹）、行内代码、代码块（深色背景）、水平线、emoji 原生保留。

---

## 六、前端 UI 设计

### 6.1 设计风格

采用类 Gemini / Modern Design 风格，深色侧边栏 + 毛玻璃头部导航栏 + 圆角卡片体系：

- **侧边栏**：深色背景 `#1e293b`，渐变 Logo，圆角菜单项
- **头部**：毛玻璃效果（`backdrop-filter: blur(12px)`），用户头像首字母 + 下拉退出
- **表格**：统一圆角 + 阴影，操作列编辑/删除按钮为纯色文字 + hover 背景
- **弹窗**：大圆角 + 深阴影，表单输入框统一聚焦紫色边框
- **全局主题**：CSS 变量体系，覆盖 Element Plus 默认样式

### 6.2 关键页面

| 页面 | 路由 | 功能 |
|------|------|------|
| 登录页 | `/login` | 深色渐变背景 + 动态光晕动画 + 磨砂玻璃卡片 |
| 仪表盘 | `/dashboard` | 欢迎语 + 功能导航卡片 + 库存表格 + AI 分析面板 |
| 员工管理 | `/employee` | 表格 + 拖拽排序 + 弹窗新增/编辑 + 批量 JSON 录入 |
| 个人信息 | `/employee/info` | 卡片 + 描述列表展示 |
| 供应商管理 | `/supplier` | 表格 + 弹窗表单（含联系人信息） |
| 商品管理 | `/product` | 表格 + 供应商外键关联 |
| 采购管理 | `/purchase` | 复合弹窗（主表表单 + 明细表格），行点击进入明细页 |
| 采购明细 | `/purchase/detail/:oId` | 订单信息卡 + 明细表 + 明细编辑弹窗 |

### 6.3 交互特性

- **拖拽排序**：列表页使用 Sortable.js，排序列持久化到 localStorage
- **批量录入**：弹窗内粘贴 JSON 数组
- **实时汇总**：采购明细数量/商品变更时实时计算总数量/总金额
- **权限控制**：普通员工侧边栏隐藏管理入口，操作按钮条件渲染

---

## 七、项目统计

| 指标 | 数值 |
|------|------|
| Git 提交数 | 31 次 |
| 总代码行数 | 5,299 行 |
| 后端 Java 文件 | 33 个 |
| MyBatis XML 映射 | 5 个 |
| 前端 Vue 组件 | 10 个 |
| 前端 JS 模块 | 9 个 |
| 数据库表 | 5 张 |
| REST API 接口 | 17 个 |
| 外键约束 | 4 条 |
| 测试用例 | 15 条（全部通过） |

---

## 八、部署说明

### 8.1 后端部署

1. 在 MySQL 中执行 `src/main/resources/db/schema.sql` 建表
2. 修改 `application.yaml` 中的数据库密码
3. 设置环境变量 `DEEPSEEK_API_KEY`（AI 分析功能需要）
4. 编译运行：
   ```bash
   cd 后端服务代码/supermarket
   ./mvnw spring-boot:run
   ```
5. 后端启动后访问 `http://localhost:8080`

### 8.2 前端部署

1. 安装依赖：
   ```bash
   cd 前端代码
   npm install
   ```
2. 启动开发服务器：
   ```bash
   npm run dev
   ```
3. 浏览器访问 `http://localhost:5173`

### 8.3 测试账号

| 工号 | 密码 | 级别 |
|------|------|------|
| E001 | 123456 | 管理员 |
| E002 | 123456 | 普通员工 |
| E003 | 123456 | 普通员工 |

---

## 九、开发日志索引

| 日期 | 内容 |
|------|------|
| 06-01 | 数据库初建，ER 图绘制 |
| 06-04 | 持久层（Mapper）全线完工 |
| 06-09 | 后端编译修复，前后端联调，登录接口打通 |
| 06-11 | 前端页面全面美化（Gemini 风格） |
| 06-12 | 全量代码审查与 Bug 修复（8 项） |
| 06-15 | AI 分析功能开发，全量功能测试，课程设计报告撰写 |
