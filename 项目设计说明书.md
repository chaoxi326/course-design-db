# 超市进销存管理系统 — 项目设计说明书

> **课程名称**：数据库系统概论课程设计  
> **项目名称**：超市进销存管理系统  
> **姓 名**：文泽仁  
> **班 级**：软件工程241  
> **学 号**：2024082123  

---

## 一、项目概述

### 1.1 项目背景

随着零售行业的快速发展，中小型超市对信息化管理的需求日益增长。传统的手工记账或 Excel 管理方式存在效率低、易出错、数据难以追溯等问题。本项目旨在开发一套基于 **B/S 架构**的超市进销存管理系统，实现供应商、商品、员工及采购订单的数字化管理。

### 1.2 项目目标

- 建立规范的关系型数据库，存储超市核心业务数据
- 提供 RESTful API 接口，支持前后端分离开发
- 实现供应商、商品、员工、采购四大模块的增删改查功能
- 支持采购订单与明细的级联操作（级联新增、级联删除）
- 为后续 Vue3 前端开发提供完整的数据接口

### 1.3 运行环境

| 类别 | 技术 | 版本 |
|---|---|---|
| 操作系统 | Windows 11 | — |
| 数据库 | MySQL | 8.0 |
| JDK | OpenJDK | 21 |
| 构建工具 | Maven | 3.9+ |
| 后端框架 | Spring Boot | 4.0.6 |
| ORM 框架 | MyBatis | 4.0.1 |
| 前端框架 | Vue 3 + Element Plus | — |
| 开发工具 | IntelliJ IDEA / VS Code | — |

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                   前端 (Vue 3)                    │
│            http://localhost:5173                 │
└─────────────────┬───────────────────────────────┘
                  │ HTTP / JSON (Axios)
                  │ CORS 已配置
┌─────────────────▼───────────────────────────────┐
│           Controller 层 (REST API)               │
│   ProductController  EmployeeController          │
│   SupplierController  PurchaseController         │
│         端口: 8080  /api/*                        │
└─────────────────┬───────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────┐
│            Service 层 (业务逻辑)                  │
│   ProductService  EmployeeService                │
│   SupplierService  PurchaseService               │
│         @Transactional 事务管理                   │
└─────────────────┬───────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────┐
│            Mapper 层 (数据访问)                   │
│   ProductMapper  EmployeeMapper                  │
│   SupplierMapper  PurchaseOrderMapper            │
│   PurchaseDetailMapper                           │
│         MyBatis XML 映射文件                      │
└─────────────────┬───────────────────────────────┘
                  │ JDBC
┌─────────────────▼───────────────────────────────┐
│           MySQL 数据库 (supermarket_db)           │
│   supplier  product  employee                    │
│   purchase_order  purchase_detail                │
└─────────────────────────────────────────────────┘
```

### 2.2 项目目录结构

```
后端服务代码/supermarket/
├── pom.xml                          # Maven 依赖配置
├── src/main/java/com/example/supermarket/
│   ├── SupermarketApplication.java  # Spring Boot 启动类
│   ├── config/
│   │   └── CorsConfig.java          # 全局 CORS 跨域配置
│   ├── entity/                      # 实体类
│   │   ├── Product.java
│   │   ├── Employee.java
│   │   ├── Supplier.java
│   │   ├── PurchaseOrder.java
│   │   └── PurchaseDetail.java
│   ├── mapper/                      # MyBatis Mapper 接口
│   │   ├── ProductMapper.java
│   │   ├── EmployeeMapper.java
│   │   ├── SupplierMapper.java
│   │   ├── PurchaseOrderMapper.java
│   │   └── PurchaseDetailMapper.java
│   ├── service/                     # 业务层接口
│   │   ├── ProductService.java
│   │   ├── EmployeeService.java
│   │   ├── SupplierService.java
│   │   └── PurchaseService.java
│   ├── service/impl/                # 业务层实现
│   │   ├── ProductServiceImpl.java
│   │   ├── EmployeeServiceImpl.java
│   │   ├── SupplierServiceImpl.java
│   │   └── PurchaseServiceImpl.java
│   └── controller/                  # 控制层
│       ├── ProductController.java
│       ├── EmployeeController.java
│       ├── SupplierController.java
│       └── PurchaseController.java
├── src/main/resources/
│   ├── application.yaml             # 应用配置（端口、数据源、MyBatis）
│   ├── mapper/                      # MyBatis XML 映射
│   │   ├── ProductMapper.xml
│   │   ├── EmployeeMapper.xml
│   │   ├── SupplierMapper.xml
│   │   ├── PurchaseOrderMapper.xml
│   │   └── PurchaseDetailMapper.xml
│   └── db/                          # SQL 脚本
│       ├── schema.sql
│       └── data.sql
└── test.http                        # 接口测试用例
```

---

## 三、数据库设计

> 详细设计请参见《数据库实验报告》，此处仅列出核心要点。

### 3.1 数据库信息

- **数据库名称**：`supermarket_db`
- **字符集**：utf8mb4
- **表数量**：5 张

### 3.2 表关系图

```
  supplier                    product
 ┌──────────┐               ┌──────────┐
 │ s_id  PK │───1:N────────│ s_id  FK │
 │ s_name   │               │ p_id  PK │
 │ s_phone  │               │ p_name   │
 │ ...      │               │ p_price  │
 └──────────┘               └────┬─────┘
                                 │ 1:N
  employee                      │
 ┌──────────┐              ┌────▼──────────┐
 │ e_id  PK │───1:N───────│purchase_detail│
 │ e_name   │              │ d_id  PK      │
 │ e_level  │              │ o_id  FK      │
 │ ...      │              │ p_id  FK      │
 └──────────┘              │ d_quantity    │
        │                  └────┬──────────┘
        │ 1:N                  │ N:1
        │              ┌───────▼───────────┐
        └─────────────│ purchase_order    │
                       │ o_id  PK         │
                       │ e_id  FK         │
                       │ o_total_price    │
                       │ o_time           │
                       └──────────────────┘
```

---

## 四、模块设计

### 4.1 供应商管理模块

| 接口 | 方法 | 路径 | 说明 |
|---|---|---|---|
| 批量录入 | POST | `/api/suppliers/batch` | 传入 JSON 数组，支持单条或批量 |
| 查询全部 | GET | `/api/suppliers` | 返回所有供应商列表 |
| 修改信息 | PUT | `/api/suppliers` | 根据 sId 更新 |
| 删除 | DELETE | `/api/suppliers/{sId}` | 有外键关联时受 RESTRICT 约束 |

### 4.2 商品管理模块

| 接口 | 方法 | 路径 | 说明 |
|---|---|---|---|
| 批量录入 | POST | `/api/products/batch` | 传入 JSON 数组 |
| 查询全部 | GET | `/api/products` | 返回所有商品列表 |
| 修改信息 | PUT | `/api/products` | 根据 pId 更新 |
| 删除 | DELETE | `/api/products/{pId}` | 已被采购的商品受 RESTRICT 约束 |

### 4.3 员工管理模块

| 接口 | 方法 | 路径 | 说明 |
|---|---|---|---|
| 批量录入 | POST | `/api/employees/batch` | 传入 JSON 数组 |
| 查询全部 | GET | `/api/employees` | 返回所有员工列表 |
| 修改信息 | PUT | `/api/employees` | 根据 eId 更新 |
| 删除 | DELETE | `/api/employees/{eId}` | 有采购记录的员工受 RESTRICT 约束 |

### 4.4 采购管理模块

| 接口 | 方法 | 路径 | 说明 |
|---|---|---|---|
| 录入采购单 | POST | `/api/purchase/batch` | 同时传 orders[] 和 details[] |
| 查询主表 | GET | `/api/purchase/orders` | 返回所有采购订单 |
| 查询明细 | GET | `/api/purchase/details` | 返回所有采购明细 |
| 修改主表 | PUT | `/api/purchase/order` | 根据 oId 更新 |
| 修改明细 | PUT | `/api/purchase/detail` | 根据 dId 更新 |
| 级联删除 | DELETE | `/api/purchase/{oId}` | 先删明细，再删主表（事务保证） |

---

## 五、技术要点

### 5.1 CORS 跨域配置

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowedOriginPatterns("*")
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(false)
                .maxAge(3600);
    }
}
```

### 5.2 MyBatis 下划线转驼峰

在 `application.yaml` 中配置：

```yaml
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
```

该配置可将数据库字段 `s_name` 自动映射为 Java 属性 `sName`，减少 SQL 中的 `AS` 别名写法。

### 5.3 事务管理

采购模块的级联操作用 `@Transactional` 注解保证数据一致性：

```java
@Transactional
public boolean saveOrdersWithDetails(List<PurchaseOrder> orders,
                                      List<PurchaseDetail> details) {
    // 主表和明细表要么同时插入成功，要么同时回滚
}

@Transactional
public boolean removeOrderWithDetails(String oId) {
    detailMapper.deleteDetailByOrderId(oId);  // 先删子表
    return orderMapper.deleteOrderById(oId) > 0;  // 再删主表
}
```

### 5.4 批量操作

所有模块的录入接口统一采用批量设计，前端传 JSON 数组，单条录入时传只含一个元素的数组即可，接口无需区分单条/批量。

```xml
<insert id="insertProductBatch">
    INSERT INTO product (p_id, p_name, p_price, s_id, p_intro, p_remark)
    VALUES
    <foreach collection="productList" item="prod" separator=",">
        (#{prod.pId}, #{prod.pName}, #{prod.pPrice}, #{prod.sId},
         #{prod.pIntro}, #{prod.pRemark})
    </foreach>
</insert>
```

---

## 六、接口测试

项目根目录下的 `test.http` 文件包含了全部 15 个接口测试用例，可在 IntelliJ IDEA 中直接运行。测试覆盖：

- 供应商：增 / 查 / 改 / 删（4 条）
- 商品：增 / 查 / 改 / 删（4 条）
- 员工：增 / 查 / 改 / 删（4 条）
- 采购：增 / 查主表 / 查明細 / 改主表 / 改明细 / 级联删除（7 条）

### 测试数据示例（商品录入）

```json
POST http://localhost:8080/api/products/batch
Content-Type: application/json

[
  {
    "pId": "P001",
    "pName": "可口可乐500ml",
    "pPrice": 3.00,
    "sId": "S001",
    "pIntro": "经典碳酸饮料，500ml瓶装",
    "pRemark": "夏季畅销品"
  }
]
```

---

## 七、待完成功能

| 序号 | 功能 | 优先级 | 状态 |
|---|---|---|---|
| 1 | Vue3 前端管理页面（4 个模块） | 高 | 🔲 待开发 |
| 2 | 员工登录接口 + 前端登录页 | 高 | 🔲 待开发 |
| 3 | 前端路由守卫（未登录拦截） | 中 | 🔲 待开发 |
| 4 | 查询分页（后端 + 前端） | 低 | 🔲 待开发 |
| 5 | 密码加密存储（MD5/SHA256） | 低 | 🔲 待优化 |
| 6 | 操作日志记录 | 低 | 🔲 待扩展 |

---

## 八、部署说明

### 8.1 后端部署

1. 在 MySQL 中创建数据库并执行建表脚本
2. 修改 `application.yaml` 中的数据库连接信息（用户名、密码）
3. 在 IDEA 中运行 `SupermarketApplication.java`
4. 后端启动后访问 `http://localhost:8080`
5. 使用 `test.http` 验证接口可用性

### 8.2 前端部署（规划中）

1. 进入前端项目目录，执行 `npm install`
2. 执行 `npm run dev` 启动开发服务器
3. 浏览器访问 `http://localhost:5173`
